#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../@netlify+config@18.2.3/node_modules/@netlify/config/src/bin/main.js" "$@"
else
  exec node  "$basedir/../../../../../@netlify+config@18.2.3/node_modules/@netlify/config/src/bin/main.js" "$@"
fi
