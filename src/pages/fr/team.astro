---
import Main from '@layouts/Main.astro'
import Container from '@components/Container.astro'
import { teamMembers } from 'src/data';

const description =
  'Das Team von Payflink wird täglich in den Restaurants motiviert... endlich bestellen und bezahlen zu dürfen.'

---

<Main title="Unser Team" description={description}>
  <Container>

    <h1>Unser Team</h1>
    <p>Das Team von Payflink wird täglich in den Restaurants motiviert... endlich bestellen und bezahlen zu dürfen.</p>
    <div class="employee-list">
      {teamMembers.map(({ name, image, role }) => (
      <div class="employee">
        <img loading="lazy" src={image} alt={name} width="192" height="192" />
        <p>
          <span class="employee-name">{name}</span>
          <br />
          {role.de}
        </p>
      </div>
      ))}
    </div>
  </Container>
</Main>

<style lang="scss">
  .employee-list {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-around;
    gap: 2em;
  }

  .employee {
    width: 16rem;
    text-align: center;

    img {
      border-radius: 1000px;
      box-shadow: 0 0 60px 0px #ccc6;
      background-color: lavender;
      aspect-ratio: 1;

      &[src$=".svg"] {
        padding-top: 1em;
      }
    }
  }

  .employee-name {
    font-size: 1.2em;
    font-weight: 600;
    color: var(--primary-color);
    margin-bottom: 0px;
  }
</style>